import template from './likeApi.html';
import controller from './likeApi.controller';
import './likeApi.scss';

let likeApiComponent = {
  bindings: {},
  template,
  controller
};

export default likeApiComponent;
