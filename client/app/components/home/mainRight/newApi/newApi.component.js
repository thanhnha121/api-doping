import template from './newApi.html';
import controller from './newApi.controller';
import './newApi.scss';

let newApiComponent = {
  bindings: {},
  template,
  controller
};

export default newApiComponent;
