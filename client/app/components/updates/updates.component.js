import template from './updates.html';
import controller from './updates.controller';
import './updates.scss';

let updatesComponent = {
  bindings: {},
  template,
  controller
};

export default updatesComponent;
